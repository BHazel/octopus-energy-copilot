{
    "servers": {
        "Octopus Energy Copilot": {
            "type": "stdio",
            "command": "${workspaceFolder}/.venv/bin/oec",
            "args": ["agent", "mcp", "run"],
            "envFile": "${workspaceFolder}/cli/cli/.env"
        }
    }
}